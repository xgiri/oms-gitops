# Apply once per environment/cluster:
#   kubectl apply -f bootstrap/root-prod.yaml --context <your-prod-context>
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: https://github.com/xgiri/gitops.git
    targetRevision: main
    path: bootstrap/children/prod
  destination:
    name: prod   # Argo CD cluster registered under this name: `argocd cluster add <context> --name prod`
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
